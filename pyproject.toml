[project]
name = "elektro-llm-agent"
version = "0.1.0"
description = "LLM agent s elektrotechnickými výpočtovými nástrojmi (tool calling)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "litellm>=1.74.9",
    "python-dotenv>=1.0.1",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
